# OR-TechTask

This project takes a user-provided input and returns a concatenation of the reverse of the input, the earliest character present in the input, and "open" if the vowel count is odd or "rent" otherwise.